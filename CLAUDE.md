Read CLAUDE.md. Set up the Next.js 15 project per the spec: create-next-app with TypeScript, install all dependencies, scaffold the file structure, split the reference component into the listed components/ files, implement app/api/tailor/route.ts with the secure server-side API call and rate limiting, set up the lib/cv-data.ts with Ahmed's master CV, configure fonts via next/font, write a README.md, and create .env.example. Stop after `npm run build` succeeds — don't deploy yet.